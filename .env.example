NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
SANITY_API_READ_TOKEN=

# When unset or when the Sanity project isn't configured yet, the app falls
# back to static mock content in src/lib/mockData.ts so it can be developed
# and reviewed locally before a real Sanity project is connected.
